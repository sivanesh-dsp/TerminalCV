; Keep this project fully self-contained (virtualenv-style isolation).
; npm's download cache lives inside the project instead of ~/.npm, and
; packages install to ./node_modules as usual. Delete the project folder
; to remove absolutely everything — nothing is written system-wide.
cache=.npm-cache
fund=false
audit=false
