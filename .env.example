# Base public path the app is served from.
#   - Vercel / custom domain / user site  -> "/" (default, leave unset)
#   - GitHub Pages project site            -> "/<repo-name>/"
# The GitHub Pages workflow sets this automatically; only set it manually for
# local production previews of a sub-path deployment.
# VITE_BASE=/terminal-resume/
